environment:
  YARN_IGNORE_ENGINES: true

  matrix:
    - nodejs_version: "9"
    - nodejs_version: "8"
    - nodejs_version: "6"

install:
  - ps: Install-Product node $env:nodejs_version
  - npm install

test_script:
  - npm run lint
  - npm run test

cache:
 - node_modules

build: off
deploy: off
