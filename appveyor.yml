environment:
  YARN_IGNORE_ENGINES: true

  matrix:
    - nodejs_version: '9'
    - nodejs_version: '8'

install:
  - ps: Install-Product node $env:nodejs_version
  - npm install -g npm@6
  - npm ci

test_script:
  - npm test

cache:
 - '%APPDATA%\npm-cache'

build: off
deploy: off
