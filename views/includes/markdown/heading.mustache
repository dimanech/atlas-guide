{{#isSection}}
    </section>
    <section class="b-atlas-section">
{{/isSection}}
<h{{level}} class="b-atlas b-atlas-title b-atlas-title_{{level}}" id="h{{level}}-{{escapedText}}">
    <span class="b-atlas-title__text">
        {{{text}}}
    </span>
    <a class="b-atlas-title__anchor" href="#h{{level}}-{{escapedText}}" title="Copy link to this title">
        <svg width="14" height="14" role="img">
            <use xlink:href="#icon-ln"></use>
        </svg>
    </a>
</h{{level}}>
